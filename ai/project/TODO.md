# Orion follow-ups

- [ ] Before merging `organize`, run `npm.cmd test` in a normal terminal. The restricted Codex Windows environment found both test files but timed out while starting Vitest fork and thread workers before loading either test.
